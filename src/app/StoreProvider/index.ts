import type {StateSchema, ThunkExtraArg, ThunkConfig} from "./config/StateSchema.ts";

export {StoreProvider} from "@/app/StoreProvider/ui/StoreProvider.tsx";

export {
    StateSchema,
    ThunkExtraArg,
    ThunkConfig
}